apiVersion: v1
kind: Service
metadata:
  name: dvwa-web
  labels:
    app: dvwa
    component: web
spec:
  selector:
    app: dvwa
    component: web
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
